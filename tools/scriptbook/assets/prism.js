/* PrismJS 1.29.0 - minimal build (core + autoloader removed)
   This is a lightweight placeholder intended for offline highlighting.
   For full language support, replace with official Prism build.
*/
(function(){
  // 极简高亮：不做真正 token 化，仅保证不报错。
  window.Prism = window.Prism || {};
  Prism.highlightAll = function(){
    // no-op fallback
  };
})();
